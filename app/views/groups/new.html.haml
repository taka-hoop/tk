=form_for @group do |f|
  = f.text_field :name, class: "chat-group-form__input", placeholder: "メッセージを入力してください"
  = f.collection_check_boxes :user_ids, User.all, :id, :name
  = f.submit "送信", class: "chat-group-form__action-btn"